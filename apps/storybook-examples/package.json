{
  "name": "storybook-examples",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "lint": "npx eslint src --fix && npx prettier --write src"
  },
  "dependencies": {
    "common": "*"
  }
}
